---
# THIS IS MY FIRST PLAYBOOK
- hosts: all 
  user: ansible
  become: yes
  connection: ssh
  tasks:
    - name: INSTALLING HTTPD SERVER ON ALL SERVERS
      action: yum name=httpd state=present

    - name: INSTALLING GIT ON ALL SERVERS
      action : yum name=git state=present
