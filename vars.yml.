---
# This is my second yaml playbook
- hosts: all
  become: yes
  vars:
    pkgname: git
  tasks:
      - name: INSTALLING GIT SERVER ON ALL SERVERS
        action: yum name='{{pkgname}}' state=present


